/*
var mongo = require('mongodb');

var db = new mongo.Db('Users', new mongo.Server('localhost', 27017), {safe: true});

db.open(function (err,db) {
  if(!err) {
    console.log("We are connected");
  }
	else {
	console.log("We aren't connected" + err);
}
});

exports.index = function(req, res) {
 res.render('index');
};

exports.index_post_handler = function(req, res) {
console.log(req.createUsername);

	db.collection('users',function(err, collection){					
	      collection.insert({"username":req.createUsername,"password":req.createPassword,"email":req.createEmail});
	});
res.render('index');
};


  db.collection('testcol', function (err, coll) {
        coll.insert({"imię": zmienna, "nazwisko": "Bratny"}, function (err) {
           
            coll.find().toArray(function (err, items) {
                console.log(items);
                db.close();
            });*/
